name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.7
  provider: ^6.0.0
  path_provider: ^2.0.0
  workmanager: ^0.9.0+3
  flutter_local_notifications: ^21.0.0
  camera: ^0.12.0+1
  shared_preferences: ^2.2.3
  record: any
  audioplayers: ^6.0.0
  http: ^1.1.0
  video_player: ^2.9.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
    - assets/cars_config.json
